<h2> Recipes search results</h2>

<table class="table table-bordered table-striped">
    <thead>
    </thead>
    <tbody>
        <% for( let index = 0; index < recipes.length; index++ ) { %>
            <% let recipe = recipes[index] %> 
            <tr>
                <th>
                    <a type="button" href=<%= `/details/${recipe.recipe.uri}` %> class="btn btn-primary">
                        <%= `See details for ID: ${recipe.recipe.uri}` %>
                    </a>
                </th>
                <td><%= recipe.recipe.label %></td>
                <td><img src=<%= recipe.recipe.image %>  alt=<%= recipe.recipe.label %>></td>
                <td><%= recipe.recipe.label %></td>
                <td><%= recipe.recipe.uri %></td>
                <td><%= recipe.recipe.ingredientLines[0] %></td>
                <td> 
                    <form method="POST" action="./results/recipes">
                    <input name="uri" type="text" value=<%= recipe.recipe.uri %> hidden >
                    <input name="label" type="text" value=<%= recipe.recipe.label %> hidden >
                    <input name="totalTime" type="text" value=<%= recipe.recipe.totalTime %> hidden >
                    <input name="calories" type="text" value=<%= recipe.recipe.calories %> hidden > 
                    <button class="btn btn-primary" type="submit"> Add Recipe</button>
                    </form>
                </td>
            </tr>

        <% } %>
    </tbody>
</table>
